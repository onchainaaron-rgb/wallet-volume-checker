{
    "rewrites": [
        {
            "source": "/api/volume/:chain/:address",
            "destination": "/api/volume.js?chain=:chain&address=:address"
        },
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}